window.addEventListener('DOMContentLoaded', () => {
    const divider = document.getElementById('divider');
    const h2Width = document.querySelector('.home h2').offsetWidth;
    divider.style.width = `${h2Width}px`;
  });
  